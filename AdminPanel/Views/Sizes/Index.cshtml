@model IEnumerable<AdminPanel.Models.Table_Size>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.s_size)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Table_Category.category_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.s_size)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Table_Category.category_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.s_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.s_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.s_id })
        </td>
    </tr>
}

</table>
